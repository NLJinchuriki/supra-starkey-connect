{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/node"
      }
    ],
    "buildCommand": "node scripts/fix-workspace-deps.js && pnpm install && pnpm run build"
  }